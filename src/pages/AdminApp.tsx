import { Box, Grid, Container, Typography } from '@mui/material';
// components
import Page from '../components/Page';

export default function AdminApp() {
    return(
        <Page title="Admin | Shopaholic">Admin Panel!</Page>
    )
}